# DESCRIPTION: Template per creare profili personalizzati
# Copia questo file e personalizza i valori secondo le tue esigenze
# Salva come profiles/mio-profilo.env per usarlo con --profile mio-profilo

# ==========================================
# AUTENTICAZIONE
# ==========================================
# Token di autenticazione (personalizza secondo l'app)
XSRF_TOKEN=""
SESSION_TOKEN=""

# ==========================================
# HEADERS BROWSER
# ==========================================
# User Agent (personalizza se necessario)
USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/140.0.0.0 Safari/537.36"

# Accept headers
ACCEPT="text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8"
ACCEPT_ENCODING="gzip, deflate, br"
ACCEPT_LANGUAGE="it-IT,it;q=0.9,en-US;q=0.8,en;q=0.7"

# Connection headers
CONNECTION="keep-alive"
UPGRADE_INSECURE_REQUESTS="1"

# ==========================================
# CONFIGURAZIONE CONNESSIONE
# ==========================================
# Timeout (adatta alla velocità della tua app)
CONNECT_TIMEOUT="10"
MAX_TIME="30"

# SSL (imposta "true" se l'app usa certificati self-signed)
INSECURE="false"
FOLLOW_REDIRECTS="true"
MAX_REDIRECTS="5"

# ==========================================
# PARAMETRI TESTING
# ==========================================
# Delay tra requests (adatta al carico che vuoi testare)
DEFAULT_DELAY="1.0"

# Jobs paralleli (adatta alle capacità del tuo server)
DEFAULT_PARALLEL_JOBS="2"

# ==========================================
# LOGGING
# ==========================================
# Livello di log (DEBUG per sviluppo, INFO per produzione)
MIN_LOG_LEVEL="INFO"

# Log headers (true per debugging, false per produzione)
LOG_HEADERS="false"

# ==========================================
# HEADERS PERSONALIZZATI
# ==========================================
# Aggiungi qui i tuoi headers specifici
# Esempio:
# X_API_KEY="la-tua-api-key"
# CUSTOM_HEADER="il-tuo-valore"